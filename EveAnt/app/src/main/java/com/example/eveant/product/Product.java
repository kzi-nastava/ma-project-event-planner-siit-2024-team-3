package com.example.eveant.product;

import com.example.eveant.service.model.Offer;

public class Product extends Offer {
    public Product() { super(); }
    // No extra fields right now; your backend Product extends Offer without new properties.
}
