<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data />

    <LinearLayout
        android:orientation="horizontal"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvCatName"
            android:textStyle="bold"
            android:textColor="#1b1b1b"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"/>

        <TextView
            android:id="@+id/tvCatDesc"
            android:textColor="#424242"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"/>

        <!-- Attach/Detach segment -->
        <LinearLayout
            android:id="@+id/segAttach"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_segment_container"
            android:padding="2dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/btnAttach"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:minWidth="88dp"
                android:gravity="center"
                android:text="Attach"
                android:textColor="@android:color/white"
                android:background="@drawable/bg_segment_left_active"
                android:paddingLeft="12dp"
                android:paddingRight="12dp" />

            <TextView
                android:id="@+id/btnDetach"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:minWidth="88dp"
                android:gravity="center"
                android:text="Detach"
                android:textColor="#3B3F58"
                android:background="@drawable/bg_segment_right_inactive"
                android:paddingLeft="12dp"
                android:paddingRight="12dp" />
        </LinearLayout>
    </LinearLayout>
</layout>
