<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Boja pozadine -->
    <solid android:color="@color/purple" />

    <!-- Zaobljeni uglovi -->
    <corners android:radius="12dp" />

    <!-- (opciono) unutrašnji padding -->
    <padding
        android:left="12dp"
        android:top="6dp"
        android:right="12dp"
        android:bottom="6dp" />
</shape>
